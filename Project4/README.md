**Project Summary**

This project builds a full binary classification pipeline for the Don’tGetKicked competition, focusing on time-based validation, custom model implementations, nonlinear features, and metric analysis (Gini, ROC AUC, PR AUC, F1 и др.).

📌 Contents
1. Data & Preprocessing
- Loaded the Don’tGetKicked dataset.
- Split data into train/validation/test strictly by purchase date (PurchDate): первые 33% дат — train, средние 33% — validation, последние 33% — test; тестовая выборка не использовалась до финальной оценки.
- Encoded categorical features using LabelEncoder / OneHotEncoder with fit только на train для предотвращения утечки данных; при появлении новых категорий в validation/test рассмотрены альтернативные count-based кодировщики.
- Normalized numeric features before training all distance- and gradient-based models.

2. Models & Metrics
- Trained LogisticRegression, GaussianNB и KNN на тренировочном наборе и оценили качество на валидации.
- Достигнут Gini-score не ниже 0.15 для лучшей из моделей; определён алгоритм с наилучшим качеством и объяснены причины (учёт линейности задачи, размерности, устойчивости к шуму и т.д.).
- Реализованы собственные функции расчёта ROC AUC и Gini по формуле Gini; проверено, что значения согласуются с sklearn.metrics.roc_auc_score по модулю.

3. Feature Engineering & Selection
- Созданы нелинейные признаки.
- Пайплайн переобучен с добавленными нелинейными признаками; получено увеличение Gini на валидационном наборе.
- На основе коэффициентов логистической регрессии определены наиболее важные признаки.
Сравнены подходы отбора признаков:
- ручное удаление признаков с малыми по модулю коэффициентами;
- L1-регуляризация для автоматического разреживания весов.

4. Advanced Models & Tuning
- Применён нелинейный SVM (например, с RBF-ядром) через sklearn.SVC (или RAPIDS при наличии GPU); при необходимости выполнена подвыборка данных для ускорения обучения.
- Проверено, позволяет ли SVM улучшить Gini на валидации относительно базовых моделей (LR, KNN, NB).
- Определена лучшая комбинация алгоритма и набора признаков (включая нелинейные фичи).
- Проведён подбор гиперпараметров для выбранной модели (например, C, gamma для SVM; C, penalty для LR; n_neighbors, metric для KNN) и определено, какие параметры сильнее всего влияют на Gini.

5. Final Evaluation & Business Metrics
- Для лучшей модели вычислены Gini на train, validation и test; проанализирован разрыв между валидационным и тестовым качеством и сделан вывод о степени переобучения или недообучения.
- Реализованы метрики Recall, Precision, F1-score и AUC PR для оценки качества по вероятностным и жёстким предсказаниям.
- Модели сравнены на тестовой выборке по AUC PR как дополнение к Gini.
- Обоснован выбор основной метрики по жёстким меткам для задачи выявления проблемных автомобилей ("lemon" cars), с учётом важности ошибок первого и второго рода (например, приоритизация высокого Recall или F1).
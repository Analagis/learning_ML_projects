Project Summary
This project implements a sequence-to-sequence name translation pipeline from English to Russian using RNN-based models, evolving from vanilla encoder-decoder to attention mechanisms and positional encodings, with comprehensive evaluation of model performance and translation quality.

📌 Contents

1. Data Preparation

- Скачан датасет с именами и разделен на train/valid/test (80-10-10%) с random_state.
- Созданы словари для английских/русских букв с поддержкой специальных токенов.
- Реализована токенизация и кодирование имен для обучения последовательностей.

2. RNN Encoder
- Обучена 1-слойная RNN (GRU/LSTM) для кодирования английских имен с early stopping по валидационной метрике.
- Сгенерировано 10 примеров русских имен с помощью обученной модели для проверки качества энкодера.

3. RNN Machine Translation
- Использован предобученный RNN Encoder + реализован RNN Decoder (built-in класс) для генерации русских имен.
- Обучен только Decoder (Encoder заморожен) с loss на предсказание следующей буквы и early stopping.
- Реализован детерминированный метод перевода (argmax для следующей буквы).
- Evaluation: графики сходимости loss (train/valid), перевод 5 train + 5 valid имен каждые n эпох, perplexity на test.

4. Attention Machine Translation
- RNN Encoder из пункта 3 + собственная реализация Attention Decoder (без built-in классов).
- Attention вычисляется между выходами энкодера и входами декодера; обучен только Decoder.
- Повторена оценка (4.e): loss curves, переводы имен, perplexity на test.
- Сравнение vanilla RNN vs Attention по реалистичности переводов и метрикам.

5. Positional Encoding Comparison
- Sine/Cosine PE: обучены Encoder, RNN Decoder и Attention Decoder с синусоидальным позиционным кодированием.
- Trainable PE: повторены те же архитектуры с обучаемыми весами позиционного кодирования + визуализация learned embeddings.
- Сравнение всех 6 моделей по perplexity, качеству переводов и скорости сходимости.

6. Multi-head Attention
- Реализован Multi-head Attention (n_heads=3) для Machine Translation.
- Добавлена визуализация attention matrix для выбранного имени каждые n эпох обучения.
- Полная оценка модели (loss curves, переводы, perplexity).
- Анализ структуры attention heads (объяснение диагонального head со сдвигом 1).
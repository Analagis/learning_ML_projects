**Project Summary**
This project implements custom tree-based models and ensemble methods for the Don’tGetKicked Kaggle competition, focusing on time-based validation, own implementations of Decision Trees, Random Forest, GBDT, and benchmarking against LightGBM, XGBoost, and CatBoost using Gini score.

📌 Contents
1. Data & Validation
- Загрузка данных Don’tGetKicked с Kaggle.
- Разбиение на train/validation/test по PurchDate с временным порядком: первые 33% дат — train, средние 33% — validation, последние 33% — test; тест не используется до финальной оценки.
- Кодирование категориальных признаков LabelEncoder / OneHotEncoder с обучением только на train, защита от data leakage.
- Рассмотрены альтернативные кодировщики (count-based) для новых категорий в validation/test.

2. Custom Tree Implementations
- Реализованы свои DecisionTreeClassifier и DecisionTreeRegressor с параметром max_depth и методами fit, predict, predict_proba.
- Отдельный класс Node: хранение подвыборки, расчёт Gini (для классификации) или стандартного отклонения (для регрессии), ссылки на левого и правого потомков.
- Реализован поиск лучшего сплита в узле по критерию Gini; на основе этого реализовано обучение полного дерева.
- Реализован Extra Randomized Tree с более случайным выбором сплитов.
- С помощью собственного дерева достигнут Gini не ниже 0.1 на валидации.
- Оценено качество sklearn.DecisionTreeClassifier на валидации и сравнение с собственной реализацией (качество, стабильность, скорость).

3. Ensembles & GBDT
- Реализован RandomForestClassifier на основе своего дерева с управлением случайностью (фиксируемый random seed).
- Случайный лес улучшил результат одиночного дерева и достиг Gini ≥ 0.15 на validation.
- Реализован GBDT-классификатор на базе собственного дерева: параметры max_depth, number_of_trees, max_features; расчёт градиента бинарной кросс-энтропии; пошаговое (инкрементальное) обучение, где каждый следующий дерево аппроксимирует остатки/градиенты предыдущих.

4. Gradient Boosting Libraries & Final Evaluation
- Обучены LightGBM, CatBoost и XGBoost на train и оценены на validation, с подбором гиперпараметров под задачу.
- Проанализированы отличия реализаций: работа с категориальными признаками в CatBoost; особенности бустинга и режим DART в XGBoost; подход LightGBM к росту деревьев и выбору сплитов.
- Определена GBDT-модель с наилучшим Gini на валидации и объяснены возможные причины её превосходства (обработка категорий, регуляризация, скорость/глубина деревьев).
- Для лучшей модели посчитан Gini на train, validation и test; проанализирован возможный спад качества на тесте и сделан вывод о наличии/отсутствии переобучения.
- Дополнительно реализован ExtraTreesClassifier, улучшивший качество одиночного дерева и достигший Gini ≥ 0.12 на валидации.
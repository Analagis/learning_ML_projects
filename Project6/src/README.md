**Project Summary**
This project explores sparse feature classification, high-dimensional visualization, and SVD-based compression and background modeling using books, handwritten digits, images, and video data.​

📌 Contents
1. Sparse Features & Classification
- Загружен Book Recommendation Dataset (пользователи, книги, рейтинги) и построена разреженная матрица взаимодействий пользователь–книга.​
- Данные разделены на train/valid/test; построены линейная модель и Random Forest для предсказания возраста пользователя по вектору взаимодействий, проведён подбор гиперпараметров.
- Выполнено понижение размерности признаков с помощью PCA и UMAP, затем обучены те же модели на сжатых признаках.
- Сравнено время обучения и качество моделей до и после снижения размерности.

2. Digit Embeddings & Visualization
- Загружен датасет MNIST (рукописные цифры 28×28, 60k train / 10k test).​
- Каждое изображение представлено как вектор пикселей и преобразовано до 2D с помощью PCA, SVD, Randomized-SVD, t-SNE, UMAP и LLE.
- Предложены метрики (например, межклассовое расстояние, внутриклассовая компактность, перекрытие кластеров) и выполнено сравнение методов по способности разделять классы цифр.

3. Image Compression with SVD
- Выбраны 3 случайных градационных изображения и загружены как матрицы интенсивностей.
- Матрицы разложены методом SVD; построены низкоранговые аппроксимации для до 10 разных рангов и визуализированы восстановленные изображения.
- Проанализирован спектр сингулярных значений и число мод, достаточных для хорошей реконструкции.
- Построены графики зависимости объяснённой дисперсии от ранга и дано объяснение понятия «объяснённая дисперсия» как доли вариации, захватываемой первыми сингулярными компонентами.

4. Video Background Detection with SVD
- Загружено видео Video_008.avi из набора Background Models Challenge (реальные сцены с фоном и движущимся передним планом).​
- Видео представлено в виде матрицы (кадры как векторы), выполнено разложение SVD.
- Первая рамка реконструирована с помощью низкоранговой аппроксимации; подобран ранг, при котором сохраняется статический фон и сглаживается движущийся объект, тем самым выделяется фон.